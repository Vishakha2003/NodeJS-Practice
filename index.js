console.log("Welcome to the NodeJS :)");

/*REPL (It feature of Node is very useful in experimenting with Node.js codes and to debug JS codes)
    Read: Reads user's input, parses input into js data struture & stores in memory
    Eval: Takes & evaluates data struture, 
    Print: Prints the result, 
    Loop: Loops the above command until the user press ctrl-c twice)

1: js Expression
2. use variables
3. Multiline code
4. use (_) to get the last result
5. we can use editor mode */